# Word Documents TODO

本文档记录当前三个 Word 开发文档中已知的过时或容易误导的内容。Word 原件暂不修改，后续有时间再统一整理。

## 1. `ERM_码垛项目功能与版本演进说明.docx`

主要问题：MovS 相关内容已经过时。

- 文档中把 `MovS版` 写成了正式版本或最终演进方向。
- 当前现场测试结论是：MovS 不可用，最终交付版已经改回非 MovS 运动实现。
- 文档里提到的 `BoxMovSCfg`、`ForwardSpeed`、MovS 轨迹文件、`ExecuteBoxForwardMovS`、MovS 调试检查项等，只能作为历史尝试记录。
- 后续修订时，应把 MovS 内容改成“历史尝试 / 未进入最终交付版”，最终运动说明以 `SafeMovJ()`、`SafeMovL()` 和分段 MovJ/MovL 逻辑为准。

## 2. `CR30HT_力传碰撞误触问题修复总结.docx`

主要问题：payload 过渡描述不是最终代码。

- 文档中部分位置写的是 `75% -> 50 ms -> 100%`。
- 当前最终代码的取料 payload 过渡为 `30% -> 50% -> 75% -> 100%`。
- 后续修订时，需要同步修改执行结论、`OpenSucker` 描述、伪代码表格和效果对比表。
- 放料阶段“释放/破真空后延迟 50 ms 再更新 payload”的方向仍然正确。

## 3. `ERM项目总结.docx`

主要问题：整体可用，但缺少隔板 I/O 边界说明。

- 文档中 DI21 隔板传感器逻辑基本正确。
- 需要补充说明：DI21 只用于隔板库存 / 料仓有无检测。
- 隔板吸取不是 DI21 控制。
- 隔板吸盘输出由 `PartCfg.Port` 控制，该端口由前端配置注入 `global.lua`。
- 当前 ERM Vin Perret 项目配置中，隔板吸盘输出为 DO16。

## 4. BIB I/O 表的共同注意点

部分 Word 表格里写的 BIB I/O 是 Lua 默认配置：

- B1 = DI1
- B2 = DI6
- B3 = DI19
- M1 = DO18
- M2 = DO19

这些可以作为 `BIBConveyorCfg` 默认值保留，但不能直接等同于最终现场运行值。最终值需要同时核对：

- 前端配置注入后的 `global.lua` 运行变量
- 项目 JSON
- 现场 I/O 监视器
- 电气图

## 5. 最终交付文档口径

后续统一修订 Word 时，建议采用以下口径：

- MovS：历史尝试，现场不可用，未进入最终交付版。
- 最终运动：非 MovS，使用 `SafeMovJ()` / `SafeMovL()` 和分段运动。
- DI21：隔板库存 / 料仓检测输入。
- `PartCfg.Port`：隔板吸盘输出，当前项目为 DO16。
- Payload：取料为 `30% -> 50% -> 75% -> 100%` 分阶段过渡。
